//
//  Test {{= typ.typename }}.swift
//  generated by staticjson parser generator
// 

import Cocoa
import XCTest

public class Test {{= typ.typename }} : XCTestCase {

    private let correctJson = self.getPositiveStr()
    private let wrongJson = self.getNegativeStr()

    override func setUp() {
        super.setUp()
        // nothing to set up
    }
    
    override func tearDown() {
        // nothing to tear down
        super.tearDown()
    }
    
    // Test-Cases
    func testParsing_Success() {
        var obj =  {{= typ.typename }}.parse(self.correctJson);
{{ for attr in typ.attributes.iter() { }}
        XCTAssert("\(obj. {{= attr.name }})" == " {{= get_test_value(&attr.attribute_type) }}", "Field:  {{= attr.name }}")
{{ } }}
    }
    
    // TODO: insert a negative Test-Case

    // Helper Functions
{{+ ./swift_template/test/get_positive_str.ct }}

{{+ ./swift_template/test/get_negative_str.ct }}
}